# FMEA 원자성 관계형 DB 아키텍처

> 작성일: 2025-12-29  
> 최종 업데이트: 2025-12-30  
> 버전: 1.0.0  
> 상태: 설계 완료

## 변경 이력

- **2025-12-30**: 고장연결 기능 완성 - 전체보기 화면 기능분석 데이터 표시 및 마지막 행 병합 추가 (v1.7.0-고장연결-완성-251230)

## 1. 개요

### 1.1 배경
기존 FMEA 시스템은 중첩(Nested) 구조로 데이터를 저장하여 다음 문제가 발생:
- 상위-하위 관계 추적 어려움
- 셀합치기(rowSpan) 시 데이터 정합성 문제
- 고장연결 확정 후 기능분석/구조분석 역전개 불가
- 여러 모듈(CP, PFD, WS, PM) 간 데이터 공유 불가

### 1.2 해결책
**원자성 관계형 DB 구조** 도입:
- 모든 데이터를 개별 레코드(Atomic Unit)로 분리
- 상위-하위 관계는 외래키(FK)로 명시적 연결
- 사용자가 입력한 데이터 그대로 저장/표시 (자동 변환 없음)
- 공유 데이터(특별특성, FMEA 결과)는 별도 테이블로 분리

---

## 2. 전체 시스템 구조

```
┌─────────────────────────────────────────────────────────────────────────────────────────┐
│                           APQP Project (최상위 컨테이너)                                  │
├─────────────────────────────────────────────────────────────────────────────────────────┤
│                                                                                          │
│  ┌───────────────────────────────────────────────────────────────────────────────────┐  │
│  │                          🔧 공정 마스터 (Process Master) - 공유                    │  │
│  │  ProcessMaster ──────────────────────────────────────────────────────────────────│  │
│  │       │                                                                           │  │
│  │       └──→ WorkElementMaster                                                      │  │
│  └───────────────────────────────────────────────────────────────────────────────────┘  │
│                    ↓ FK 참조                    ↓ FK 참조                                │
│  ┌─────────────────────────────────┐  ┌─────────────────────────────────┐              │
│  │  ⭐ 특별특성 (Special Char)     │  │  📊 FMEA 결과 (FMEAResult)       │              │
│  │  - 공유 마스터                  │  │  - DFMEA/PFMEA 분석 결과         │              │
│  │  - 모든 모듈에서 FK 참조        │  │  - SOD, AP 값                    │              │
│  │  - SpecialCharUsage로 추적     │  │  - 예방/검출 조치                │              │
│  └─────────────────────────────────┘  └─────────────────────────────────┘              │
│         ↓ FK                                  ↓ FK                                      │
│  ┌──────────────────────────────────────────────────────────────────────────────────┐  │
│  │                              📋 개별 모듈 (원자성 DB)                              │  │
│  ├──────────────────────────────────────────────────────────────────────────────────┤  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────────┐ │  │
│  │  │   DFMEA    │  │   PFMEA    │  │     CP     │  │    PFD     │  │  WS / PM   │ │  │
│  │  └────────────┘  └────────────┘  └────────────┘  └────────────┘  └────────────┘ │  │
│  └──────────────────────────────────────────────────────────────────────────────────┘  │
│                                                                                          │
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. PFMEA 원자성 DB 스키마

### 3.1 구조분석 (2단계)

```typescript
// 1L 완제품 공정 (최상위)
interface L1Structure {
  id: string;           // PK
  fmeaId: string;       // FK: FMEA 프로젝트 ID
  name: string;         // 완제품 공정명
  confirmed?: boolean;  // 확정 상태
}

// 2L 메인공정
interface L2Structure {
  id: string;           // PK
  fmeaId: string;       // FK: FMEA 프로젝트 ID
  l1Id: string;         // FK: L1Structure.id (상위)
  no: string;           // 공정 번호
  name: string;         // 공정명
  order: number;        // 순서
}

// 3L 작업요소
interface L3Structure {
  id: string;           // PK
  fmeaId: string;       // FK: FMEA 프로젝트 ID
  l1Id: string;         // FK: L1Structure.id
  l2Id: string;         // FK: L2Structure.id (상위)
  m4: 'MN' | 'MC' | 'IM' | 'EN' | '';  // 4M 분류
  name: string;         // 작업요소명
  order: number;
}
```

### 3.2 기능분석 (3단계)

```typescript
// 1L 완제품 기능 (구분 → 기능 → 요구사항 통합)
interface L1Function {
  id: string;           // PK
  fmeaId: string;       // FK
  l1StructId: string;   // FK: L1Structure.id (상위 구조분석)
  category: 'Your Plant' | 'Ship to Plant' | 'User';
  functionName: string;
  requirement: string;  // 원자 단위
}

// 2L 메인공정 기능 (기능 → 제품특성 통합)
interface L2Function {
  id: string;           // PK
  fmeaId: string;       // FK
  l2StructId: string;   // FK: L2Structure.id (상위 구조분석)
  functionName: string;
  productChar: string;  // 제품특성 (원자 단위)
  specialChar?: string;
}

// 3L 작업요소 기능 (기능 → 공정특성 통합)
interface L3Function {
  id: string;           // PK
  fmeaId: string;       // FK
  l3StructId: string;   // FK: L3Structure.id (상위 구조분석)
  l2StructId: string;   // FK: 연결용
  functionName: string;
  processChar: string;  // 공정특성 (원자 단위)
  specialChar?: string;
}
```

### 3.3 고장분석 (4단계)

```typescript
// 고장영향 (FE)
interface FailureEffect {
  id: string;           // PK
  fmeaId: string;       // FK
  l1FuncId: string;     // FK: L1Function.id (상위 기능분석)
  category: 'Your Plant' | 'Ship to Plant' | 'User';
  effect: string;
  severity: number;     // 1-10
}

// 고장형태 (FM) - 중심축
interface FailureMode {
  id: string;           // PK
  fmeaId: string;       // FK
  l2FuncId: string;     // FK: L2Function.id (상위 기능분석)
  l2StructId: string;   // FK: L2Structure.id (역전개용)
  mode: string;
  specialChar?: boolean;
}

// 고장원인 (FC)
interface FailureCause {
  id: string;           // PK
  fmeaId: string;       // FK
  l3FuncId: string;     // FK: L3Function.id (상위 기능분석)
  l3StructId: string;   // FK: L3Structure.id (역전개용)
  l2StructId: string;   // FK: 연결용
  cause: string;
  occurrence?: number;  // 1-10
}
```

### 3.4 고장연결 (관계 테이블)

```typescript
// FM을 중심으로 FE, FC 연결
interface FailureLink {
  id: string;           // PK
  fmeaId: string;       // FK
  fmId: string;         // FK: FailureMode.id (중심)
  feId: string;         // FK: FailureEffect.id
  fcId: string;         // FK: FailureCause.id
  cache?: {             // 조회 성능 최적화
    fmText: string;
    fmProcess: string;
    feText: string;
    feCategory: string;
    feSeverity: number;
    fcText: string;
    fcWorkElem: string;
    fcProcess: string;
  };
}
```

---

## 4. FK 관계도 (상하 관계)

```
구조분석 (2단계)                 기능분석 (3단계)                 고장분석 (4단계)
──────────────────────────────────────────────────────────────────────────────────

L1Structure ────────FK────→ L1Function ────────FK────→ FailureEffect (FE)
(완제품 공정)         l1StructId    (요구사항)          l1FuncId     (고장영향)
     │                                                          ↑
     │ l1Id (FK)                                               │
     ↓                                                          │ fmId (FK)
L2Structure ────────FK────→ L2Function ────────FK────→ FailureMode (FM) ←───┐
(메인공정)           l2StructId    (제품특성)          l2FuncId    (고장형태)   │
     │                                                          │           │
     │ l2Id (FK)                                               │ fmId (FK) │
     ↓                                                          ↓           │
L3Structure ────────FK────→ L3Function ────────FK────→ FailureCause (FC) ───┘
(작업요소)           l3StructId    (공정특성)          l3FuncId    (고장원인)
```

---

## 5. 공유 데이터 스키마

### 5.1 특별특성 (SpecialCharacteristic)

```typescript
interface SpecialCharacteristic {
  id: string;
  apqpId: string;       // FK: APQPProject.id
  type: 'CC' | 'SC' | 'HIC' | 'SIC' | '';
  symbol: string;       // 특성 기호 (◇, ◆, ○, ●)
  name: string;
  specification?: string;
  tolerance?: string;
  sourceType: 'DFMEA' | 'PFMEA' | 'Customer' | 'Internal' | 'Regulation';
  sourceId?: string;    // FK: 원천 FMEA ID
  processId?: string;   // FK: ProcessMaster.id
  workElementId?: string; // FK: WorkElementMaster.id
  status: 'draft' | 'approved' | 'active' | 'obsolete';
}

// 사용 추적
interface SpecialCharUsage {
  id: string;
  specialCharId: string;  // FK
  moduleType: 'DFMEA' | 'PFMEA' | 'CP' | 'PFD' | 'WS' | 'PM';
  moduleItemId: string;   // FK: 해당 모듈 항목 ID
  apqpId: string;
}
```

### 5.2 FMEA 결과 (FMEAResult)

```typescript
interface FMEAResult {
  id: string;
  apqpId: string;         // FK
  fmeaType: 'DFMEA' | 'PFMEA';
  fmeaId: string;         // FK
  processId: string;      // FK: ProcessMaster.id
  workElementId?: string; // FK
  
  // 고장분석 결과
  failureMode: string;
  failureEffect: string;
  failureCause: string;
  
  // SOD
  severity: number;
  occurrence: number;
  detection: number;
  ap: 'H' | 'M' | 'L';
  
  // 조치
  preventionControl: string;
  detectionControl: string;
  
  specialCharId?: string; // FK: SpecialCharacteristic.id
  status: 'analyzed' | 'action_required' | 'action_completed' | 'verified';
}
```

---

## 6. 모듈별 원자성 DB

### 6.1 Control Plan (CP)

| 테이블 | 설명 | FK 참조 |
|--------|------|---------|
| ControlPlan | CP 헤더 | apqpId |
| ControlPlanItem | CP 항목 (원자) | cpId, processId, fmeaResultId, specialCharId |

### 6.2 Process Flow Diagram (PFD)

| 테이블 | 설명 | FK 참조 |
|--------|------|---------|
| ProcessFlowDiagram | PFD 헤더 | apqpId |
| PFDItem | PFD 항목 (원자) | pfdId, processId, fmeaResultId, specialCharId |

### 6.3 Work Standard (WS)

| 테이블 | 설명 | FK 참조 |
|--------|------|---------|
| WorkStandard | WS 헤더 | apqpId, processId |
| WorkStandardItem | WS 항목 (원자) | wsId, workElementId, fmeaResultId, specialCharId |

### 6.4 Preventive Maintenance (PM)

| 테이블 | 설명 | FK 참조 |
|--------|------|---------|
| PreventiveMaintenance | PM 헤더 | apqpId, equipmentId |
| PMItem | PM 항목 (원자) | pmId, processId, fmeaResultId, specialCharId |

---

## 7. 핵심 원칙

### 7.1 데이터 입력 흐름 (정전개)

```
사용자 입력 순서:

1. 구조분석 (2단계)
   ├── L1Structure: 완제품 공정명 입력 → 저장
   ├── L2Structure: 메인공정 입력 (l1Id FK 연결) → 저장
   └── L3Structure: 작업요소 입력 (l2Id FK 연결) → 저장

2. 기능분석 (3단계)
   ├── L1Function: 구분/기능/요구사항 입력 (l1StructId FK 선택) → 저장
   ├── L2Function: 메인공정 기능/제품특성 입력 (l2StructId FK 선택) → 저장
   └── L3Function: 작업요소 기능/공정특성 입력 (l3StructId FK 선택) → 저장

3. 고장분석 (4단계)
   ├── FailureEffect: 고장영향 입력 (l1FuncId FK 선택) → 저장
   ├── FailureMode: 고장형태 입력 (l2FuncId FK 선택) → 저장
   └── FailureCause: 고장원인 입력 (l3FuncId FK 선택) → 저장

4. 고장연결 (4단계)
   └── FailureLink: FM-FE-FC 관계 확정 → 저장

5. 전체보기/평가탭
   └── 저장된 FK 관계를 따라 DB에서 조회 → 그대로 표시
```

### 7.2 금지 사항

- ❌ **자동 텍스트 변환 금지**: 고장 → 기능 자동 변환 하지 않음
- ❌ **임의 데이터 생성 금지**: 사용자가 입력하지 않은 데이터 자동 생성 안 함
- ❌ **중첩 구조 사용 금지**: 모든 데이터는 개별 테이블에 원자적으로 저장

### 7.3 필수 사항

- ✅ **FK 관계 명시**: 모든 상위-하위 관계는 FK로 저장
- ✅ **사용자 입력 그대로 저장**: 입력값 변경 없이 저장
- ✅ **저장된 값 그대로 표시**: FK 조회 결과 그대로 화면에 표시

---

## 8. 파일 구조

```
src/
├── lib/
│   └── shared-schema.ts        # 공유 원자성 DB 스키마
│       ├── ProcessMaster       # 공정 마스터
│       ├── WorkElementMaster   # 작업요소 마스터
│       ├── SpecialCharacteristic # 특별특성 (공유)
│       ├── FMEAResult          # FMEA 결과 (공유)
│       ├── ControlPlan/Item    # CP
│       ├── PFD/PFDItem         # PFD
│       ├── WorkStandard/Item   # WS
│       └── PM/PMItem           # PM
│
├── app/pfmea/worksheet/
│   ├── schema.ts               # PFMEA 원자성 DB 스키마
│   │   ├── L1/L2/L3 Structure  # 구조분석
│   │   ├── L1/L2/L3 Function   # 기능분석
│   │   ├── FailureEffect/Mode/Cause # 고장분석
│   │   ├── FailureLink         # 고장연결
│   │   └── getLinkedDataByFK() # FK 기반 조회
│   │
│   ├── migration.ts            # 마이그레이션 유틸리티
│   │   ├── migrateToAtomicDB() # 기존→원자성 변환
│   │   ├── convertToLegacyFormat() # 원자성→레거시 변환
│   │   ├── loadWorksheetDB()   # 로드
│   │   ├── saveWorksheetDB()   # 저장
│   │   └── confirmFailureLink() # 고장연결 확정
│   │
│   ├── hooks/
│   │   └── useWorksheetState.ts # 상태 관리 Hook
│   │       ├── atomicDB        # 원자성 DB 상태
│   │       ├── flattenedRows   # 평탄화된 행 데이터
│   │       └── saveAtomicDB()  # 저장 함수
│   │
│   └── constants.ts            # 상수 정의 (레거시 호환)
```

---

## 9. 유틸리티 함수

### 9.1 FK 기반 데이터 조회

```typescript
import { getLinkedDataByFK } from './schema';

// 고장연결 결과를 FK 관계 따라 조회
const { rows } = getLinkedDataByFK(atomicDB);

// rows에는 다음 정보 포함:
// - 구조분석: l1StructName, l2StructNo, l2StructName, l3StructM4, l3StructName
// - 기능분석: l1Category, l1FuncName, l1Requirement, l2FuncName, l2ProductChar, l3FuncName, l3ProcessChar
// - 고장분석: feCategory, feText, feSeverity, fmText, fcText, fcOccurrence
// - 셀합치기: fmId, fmRowSpan, l2RowSpan
```

### 9.2 FK 연결 저장

```typescript
import { linkFunctionToStructure, linkFailureToFunction } from './schema';

// 기능분석 → 구조분석 FK 연결
const db1 = linkFunctionToStructure(db, 2, 'l2Func_001', 'l2Struct_001');

// 고장분석 → 기능분석 FK 연결
const db2 = linkFailureToFunction(db1, 'FM', 'fm_001', 'l2Func_001');
```

### 9.3 특별특성 관리

```typescript
import { addSpecialChar, registerSpecialCharUsage, getSpecialCharsForModule } from './shared-schema';

// 특별특성 추가
const db1 = addSpecialChar(sharedDB, {
  apqpId: 'apqp_001',
  type: 'CC',
  symbol: '◇',
  name: '용접 강도',
  sourceType: 'PFMEA',
  status: 'approved',
});

// CP에서 사용 등록
const db2 = registerSpecialCharUsage(db1, 'sc_001', 'CP', 'cpItem_001', 'apqp_001');

// 모듈별 특별특성 조회
const cpSpecialChars = getSpecialCharsForModule(db2, 'CP', 'apqp_001');
```

---

## 10. 마이그레이션

### 10.1 기존 데이터 → 원자성 DB

```typescript
import { loadWorksheetDB } from './migration';

// 자동 마이그레이션 (로드 시 감지)
const atomicDB = loadWorksheetDB(fmeaId);
// - 원자성 DB 발견 시: 그대로 반환
// - 레거시 발견 시: 마이그레이션 후 반환
```

### 10.2 레거시 호환

```typescript
import { convertToLegacyFormat } from './migration';

// 기존 화면 코드 호환
const legacyData = convertToLegacyFormat(atomicDB);
// → { l1, l2, failureLinks, structureConfirmed, ... }
```

---

## 11. 단계별 검증 시스템

### 11.1 검증 함수

```typescript
import { 
  validateStructure,   // 구조분석 검증
  validateFunction,    // 기능분석 검증
  validateFailure,     // 고장분석 검증
  validateFailureLink, // 고장연결 검증
  validateAll,         // 전체 검증
  confirmStep,         // 단계 확정 (검증 + DB 업데이트)
} from './schema';
```

### 11.2 검증 결과 인터페이스

```typescript
interface ValidationResult {
  isValid: boolean;       // 검증 통과 여부
  errors: ValidationError[];    // 에러 목록 (확정 불가)
  warnings: ValidationError[];  // 경고 목록 (확정 가능, 주의 필요)
  missingCount: number;   // 누락 항목 수
  totalCount: number;     // 전체 항목 수
}

interface ValidationError {
  level: 'error' | 'warning';
  field: string;          // 필드명
  message: string;        // 메시지
  itemId?: string;        // 해당 항목 ID
}
```

### 11.3 단계별 검증 내용

| 단계 | 검증 내용 | 에러 조건 |
|------|-----------|-----------|
| **구조분석** | L1 공정명, L2 공정명, L3 작업요소명 | 이름 누락 시 |
| **기능분석** | 구분, 기능명, 요구사항/제품특성/공정특성, FK | 값 누락 또는 FK 없음 |
| **고장분석** | 고장영향/형태/원인, 심각도, FK | 값 누락 또는 FK 없음 |
| **고장연결** | 모든 FM이 FE/FC와 연결 | 미연결 FM 있음 |

### 11.4 사용 예시

```typescript
// 구조분석 확정 시
const result = confirmStep(atomicDB, 'structure');

if (!result.success) {
  // 경고 메시지 표시
  alert(result.message);
  // result.validation.errors 에서 상세 오류 확인
  return;
}

// 확정 성공 - DB 업데이트 및 저장
saveWorksheetDB(result.db);
alert(result.message);
```

### 11.5 전체 검증

```typescript
// 전체 FMEA 검증
const allValidation = validateAll(atomicDB);

console.log('전체 결과:', allValidation.overall);
// { isValid: false, totalErrors: 5, totalWarnings: 3, totalMissing: 8 }

// 단계별 확인
if (!allValidation.structure.isValid) {
  console.log('구조분석 오류:', allValidation.structure.errors);
}
if (!allValidation.function.isValid) {
  console.log('기능분석 오류:', allValidation.function.errors);
}
```

---

## 12. 표준 용어 정의

### 4M 분류 (고장원인 분류)
| 약어 | 원어 | 한글 | 설명 |
|------|------|------|------|
| MN | Man | 사람 | 작업자 관련 원인 |
| MC | Machine | 설비 | 설비/장비 관련 원인 |
| IM | In-Material | 부자재 | 부자재 관련 원인 |
| EN | Environment | 환경 | 작업환경 관련 원인 |

### 구분 (고장영향 범위)
| 명칭 | 한글 | 설명 |
|------|------|------|
| Your Plant | 자사 | 자사 공정 내 영향 |
| Ship to Plant | 고객사 | 고객 공장 영향 |
| User | 사용자 | 최종 사용자 영향 |

⚠️ **EU 사용 금지** - 유럽연합과 혼동 → User 그대로 사용

### FMEA 약어
| 약어 | 원어 | 한글 |
|------|------|------|
| FE | Failure Effect | 고장영향 |
| FM | Failure Mode | 고장형태 |
| FC | Failure Cause | 고장원인 |
| PC | Prevention Control | 예방관리 |
| DC | Detection Control | 검출관리 |
| S | Severity | 심각도 (1-10) |
| O | Occurrence | 발생도 (1-10) |
| D | Detection | 검출도 (1-10) |
| AP | Action Priority | 조치우선순위 (H/M/L) |
| RPN | Risk Priority Number | 위험우선순위 (S×O×D) |
| SC | Special Characteristic | 특별특성 |

---

## 13. 핵심 룰 (1순위) ⚠️

### 절대 준수 사항

1. **FK 기반 저장**
   - 구조분석 → 기능분석 → 고장분석 순서로 진행
   - 각 단계에서 상위 FK를 명시적으로 저장
   - 자동 변환/자동 생성 절대 금지

2. **모달 상위 고정 표시** 🔥
   - 모든 입력 모달에서 상위 항목을 **고정 배지로 표시**
   - 드롭다운/검색 아님! 이미 테이블에서 정해진 상위 항목 고정 표시
   
   | 모달 | 상위 항목 (고정 표시) | FK 저장 |
   |------|----------------------|---------|
   | 기능분석 L1 | 구조분석 L1 (완제품 공정명) | `l1StructId` |
   | 기능분석 L2 | 구조분석 L2 (메인공정명) | `l2StructId` |
   | 기능분석 L3 | 구조분석 L3 (작업요소명) | `l3StructId` |
   | 고장분석 FE | 기능분석 L1 (요구사항) | `l1FuncId` |
   | 고장분석 FM | 기능분석 L2 (제품특성) | `l2FuncId` |
   | 고장분석 FC | 기능분석 L3 (공정특성) | `l3FuncId` |

   **예시 (기능분석 L2 모달):**
   ```
   ┌────────────────────────────────────────────┐
   │  메인공정 기능 입력                         │
   ├────────────────────────────────────────────┤
   │  📌 상위: [수입검사] (공정명 고정 배지)      │
   │                                            │
   │  기능명: [________________]                │
   │  제품특성: [________________]              │
   │  특별특성: [드롭다운 ▼]                     │
   │                                            │
   │  [저장] [취소]                              │
   └────────────────────────────────────────────┘
   ```

3. **단계별 검증**
   - 각 단계 확정 전 `validateXXX()` 함수로 검증
   - 누락 항목 있으면 경고 메시지 발생
   - 검증 실패 시 확정 불가

4. **즉시 DB 업데이트**
   - 확정/저장 시 `saveWorksheetDB()` 즉시 호출
   - 원자성 DB + 레거시 DB 동시 저장
   - 상위 FK 반드시 함께 저장

5. **공유 데이터 FK 참조**
   - 특별특성, FMEA 결과는 공유 DB에 저장
   - CP/PFD/WS/PM에서 FK로 참조

---

## 13. 향후 작업

1. [ ] 기능분석 탭에서 FK 선택 UI 추가
2. [ ] 고장분석 탭에서 FK 선택 UI 추가
3. [ ] 검증 결과 UI 표시 (에러/경고 하이라이트)
4. [ ] CP/PFD/WS/PM 모듈 원자성 DB 적용
5. [ ] 특별특성 마스터 관리 화면 개발
6. [ ] FMEA 결과 공유 기능 개발

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|-----------|
| 1.0.0 | 2025-12-29 | 초기 설계 완료 |
| 1.1.0 | 2025-12-29 | 단계별 검증 시스템 추가 (validateXXX, confirmStep) |
| 1.2.0 | 2025-12-29 | 핵심 룰 (1순위) 섹션 추가, 자동변환 금지 명시 |

