# 🎨 P-FMEA 색상 표준 v2.0

> **작성일**: 2025-12-30  
> **버전**: v2.0 (개선판)  
> **변경 사유**: 색상 단순화, 붉은색 제거, 부드러운 톤 적용

---

## 📊 색상 철학

### 기본 원칙
1. **3가지 색상만 사용**: 구조분석, 기능분석, 고장분석
2. **부드러운 톤**: 너무 진하지 않은 accent 색상
3. **붉은색 제거**: 불편한 색상 배제
4. **일관성**: 모든 화면에서 동일한 색상 사용

---

## 🎯 메인 색상 (3가지만)

### 1. 구조분석 색상 (파란색 계열)
```typescript
structure: {
  main: '#42a5f5',        // 밝은 파란색 (메인)
  light: '#e3f2fd',       // 배경 (짝수행)
  dark: '#1976d2',        // 테두리/강조
  text: '#0d47a1',        // 텍스트
  zebra: '#bbdefb'        // 홀수행 (줄무늬)
}
```

**사용처**: 
- 구조분석 탭 헤더
- 완제품/공정/작업요소 테이블
- 고장연결의 구조 정보

---

### 2. 기능분석 색상 (초록색 계열)
```typescript
function: {
  main: '#66bb6a',        // 밝은 초록색 (메인)
  light: '#e8f5e9',       // 배경 (짝수행)
  dark: '#388e3c',        // 테두리/강조
  text: '#1b5e20',        // 텍스트
  zebra: '#c8e6c9'        // 홀수행 (줄무늬)
}
```

**사용처**:
- 기능분석 탭 헤더
- 기능/요구사항/제품특성/공정특성 테이블
- 고장연결의 기능 정보

---

### 3. 고장분석 색상 (주황색 계열) ⭐ 변경
```typescript
failure: {
  main: '#ffa726',        // 부드러운 주황색 (메인) ← 붉은색 대체
  light: '#fff3e0',       // 배경 (짝수행)
  dark: '#f57c00',        // 테두리/강조
  text: '#e65100',        // 텍스트
  zebra: '#ffe0b2'        // 홀수행 (줄무늬)
}
```

**사용처**:
- 고장분석 탭 헤더
- FE/FM/FC 테이블
- 고장연결 전체

**변경 이유**:
- ❌ 기존 #c62828 (빨간색) → 불편함, 경고 느낌
- ✅ 신규 #ffa726 (주황색) → 부드럽고 따뜻한 느낌

---

## 🚫 사용하지 않는 색상

### 제거된 색상
```typescript
// ❌ 제거: 리스크분석 보라색
risk: '#6a1b9a'

// ❌ 제거: 최적화 주황색  
opt: '#e65100'

// ❌ 제거: 기타 다양한 색상들
```

**이유**: 3가지 색상으로 충분, 색상 혼란 방지

---

## 📐 적용 가이드

### 줄무늬 패턴 (Zebra Striping) 🦓

> **모든 테이블에 필수 적용**

테이블 행의 가독성 향상을 위해 홀수/짝수 행에 다른 배경색을 적용합니다.

```typescript
// 줄무늬 적용 방식
const zebraBg = idx % 2 === 1 ? COLORS.[category].zebra : COLORS.[category].light;

// 예시: 구조분석 테이블
<tr style={{ background: idx % 2 === 1 ? COLORS.structure.zebra : COLORS.structure.light }}>
  ...
</tr>

// 예시: 기능분석 테이블
<tr style={{ background: idx % 2 === 1 ? COLORS.function.zebra : COLORS.function.light }}>
  ...
</tr>

// 예시: 고장분석 테이블
<tr style={{ background: idx % 2 === 1 ? COLORS.failure.zebra : COLORS.failure.light }}>
  ...
</tr>
```

**줄무늬 색상:**
| 분류 | 짝수행 (light) | 홀수행 (zebra) |
|------|---------------|---------------|
| 구조분석 | `#e3f2fd` | `#bbdefb` |
| 기능분석 | `#e8f5e9` | `#c8e6c9` |
| 고장분석 | `#fff3e0` | `#ffe0b2` |
| 일반 | `#fff` | `#f5f5f5` |

---

### 색상 사용 규칙

#### 헤더 (Header)
```typescript
background: main        // #42a5f5, #66bb6a, #ffa726
color: white
fontSize: 12px
fontWeight: 700
```

#### 서브헤더 (Sub-header)
```typescript
background: light       // #e3f2fd, #e8f5e9, #fff3e0
color: dark             // #1976d2, #388e3c, #f57c00
fontSize: 11px
fontWeight: 600
```

#### 테이블 셀
```typescript
background: light       // 연한 배경
border: 1px solid dark  // 진한 테두리
color: text             // 텍스트
fontSize: 10px
fontWeight: 400
```

#### 배지/강조
```typescript
background: main
color: white
fontSize: 10px
fontWeight: 700
padding: 2px 6px
borderRadius: 3px
```

---

## 🔄 기존 코드 변경 예시

### Before (v1.0 - 5가지 색상)
```typescript
const COLORS = {
  structure: '#1976d2',   // 구조분석
  function: '#1b5e20',    // 기능분석
  failure: '#c62828',     // 고장분석 (빨간색) ❌
  risk: '#6a1b9a',        // 리스크 ❌
  opt: '#e65100'          // 최적화 ❌
};
```

### After (v2.0 - 3가지 색상)
```typescript
const COLORS = {
  structure: {
    main: '#42a5f5',      // 밝은 파란색
    light: '#e3f2fd',
    dark: '#1976d2',
    text: '#0d47a1'
  },
  function: {
    main: '#66bb6a',      // 밝은 초록색
    light: '#e8f5e9',
    dark: '#388e3c',
    text: '#1b5e20'
  },
  failure: {
    main: '#ffa726',      // 부드러운 주황색 ✅
    light: '#fff3e0',
    dark: '#f57c00',
    text: '#e65100'
  }
};
```

---

## 📋 적용 우선순위

### Phase 1: 핵심 컴포넌트 (즉시)
1. ✅ `constants.ts` - 색상 정의 변경
2. ✅ `FailureLinkTab.tsx` - 고장연결 색상
3. ✅ 모든 분석 탭 헤더

### Phase 2: 세부 컴포넌트
4. ✅ 모달 색상
5. ✅ 트리뷰 색상
6. ✅ 배지/라벨 색상

---

## 🎨 색상 팔레트 요약

```
┌─────────────────────────────────────────┐
│ 구조분석 (파란색)                        │
│ ████ #42a5f5 (메인)                     │
│ ░░░░ #e3f2fd (배경)                     │
├─────────────────────────────────────────┤
│ 기능분석 (초록색)                        │
│ ████ #66bb6a (메인)                     │
│ ░░░░ #e8f5e9 (배경)                     │
├─────────────────────────────────────────┤
│ 고장분석 (주황색) ⭐ 신규               │
│ ████ #ffa726 (메인)                     │
│ ░░░░ #fff3e0 (배경)                     │
└─────────────────────────────────────────┘
```

---

## 🔍 색상 비교

| 항목 | v1.0 (기존) | v2.0 (개선) | 변경 이유 |
|------|-------------|-------------|-----------|
| 구조분석 | #1976d2 (진한 파란색) | #42a5f5 (밝은 파란색) | 부드러운 톤 |
| 기능분석 | #1b5e20 (진한 초록색) | #66bb6a (밝은 초록색) | 부드러운 톤 |
| 고장분석 | #c62828 (빨간색) ❌ | #ffa726 (주황색) ✅ | 불편한 색 제거 |
| 리스크 | #6a1b9a (보라색) | 제거 | 단순화 |
| 최적화 | #e65100 (주황색) | 제거 | 단순화 |

---

## ✅ 적용 체크리스트

### 파일별 변경 사항
- [ ] `src/app/pfmea/worksheet/constants.ts`
- [ ] `src/app/pfmea/worksheet/tabs/failure/FailureLinkTab.tsx`
- [ ] `src/app/pfmea/worksheet/tabs/failure/FailureL1Tab.tsx`
- [ ] `src/app/pfmea/worksheet/tabs/failure/FailureL2Tab.tsx`
- [ ] `src/app/pfmea/worksheet/tabs/failure/FailureL3Tab.tsx`
- [ ] `src/app/pfmea/worksheet/tabs/function/*.tsx`
- [ ] `src/components/modals/*.tsx`

---

## 🌈 최종 색상 코드

```typescript
// src/app/pfmea/worksheet/constants.ts
export const COLORS = {
  // 구조분석 (파란색)
  structure: {
    main: '#42a5f5',
    light: '#e3f2fd',     // 짝수행
    dark: '#1976d2',
    text: '#0d47a1',
    zebra: '#bbdefb'      // 홀수행 🦓
  },
  // 기능분석 (초록색)
  function: {
    main: '#66bb6a',
    light: '#e8f5e9',     // 짝수행
    dark: '#388e3c',
    text: '#1b5e20',
    zebra: '#c8e6c9'      // 홀수행 🦓
  },
  // 고장분석 (주황색)
  failure: {
    main: '#ffa726',
    light: '#fff3e0',     // 짝수행
    dark: '#f57c00',
    text: '#e65100',
    zebra: '#ffe0b2'      // 홀수행 🦓
  },
  // 공통 색상
  bg: '#f5f7fb',
  text: '#333',
  line: '#e0e0e0',
  white: '#fff',
  gray: '#9e9e9e'
};
```

---

**✨ 3가지 부드러운 색상으로 깔끔하고 편안한 화면을 만듭니다!**


