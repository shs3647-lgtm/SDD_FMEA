http://localhost:3000/pfmea/worksheet?v=1.0.21-20260102-auto-unconfirm-tdd

=== VERSION INFO ===
Version: 1.0.21
Last Updated: 2025-01-02
Changes:
  - [TDD 완료] 모든 탭에 데이터 불일치 자동 수정 로직 추가
  - missingCount > 0인데 확정 상태이면 자동으로 확정 해제
  - 사용자가 "수정" 버튼 없이도 바로 기능 추가 가능

=== 적용된 파일 (6개) ===
✅ FunctionL1Tab.tsx - 자동 확정 해제 추가
✅ FunctionL2Tab.tsx - 자동 확정 해제 추가
✅ FunctionL3Tab.tsx - 자동 확정 해제 추가
✅ FailureL1Tab.tsx - 자동 확정 해제 추가
✅ FailureL2Tab.tsx - 자동 확정 해제 추가
✅ FailureL3Tab.tsx - 자동 확정 해제 추가

=== 로직 ===
useEffect(() => {
  if (isConfirmed && missingCount > 0) {
    setState(prev => ({ ...prev, xxxConfirmed: false }));
    setDirty(true);
    saveToLocalStorage?.();
  }
}, [isConfirmed, missingCount, setState, setDirty, saveToLocalStorage]);

=== 테스트 시나리오 ===
1. 확정된 상태에서 누락 항목이 있으면 자동으로 확정 해제
2. 셀 클릭 시 모달 열림
3. 모달에서 선택 후 "적용" 클릭
4. 새 항목 추가됨
5. 브라우저 콘솔에서 "데이터 불일치 감지: 확정 상태이지만 누락 항목 존재, 자동 해제" 로그 확인
